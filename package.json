{
  "name": "decksmith-site",
  "version": "1.0.0",
  "private": true,
  "description": "DeckSmith — brand, product story and support site. Static, no dependencies.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npx --yes serve public",
    "build": "node scripts/build.mjs"
  }
}
